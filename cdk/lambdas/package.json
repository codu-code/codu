{
  "name": "lambdas",
  "version": "1.0.0",
  "description": "",
  "main": "imageResize.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && esbuild ./src/* --entry-names=[dir]/[name]/index --bundle --minify --sourcemap --platform=node --outdir=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.391.0",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "esbuild": "^0.19.2"
  }
}
